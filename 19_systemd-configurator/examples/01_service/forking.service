[Unit]
Description=Forking type service demo

[Service]
Type=forking
PIDFile=/run/demo.pid
ExecStart=/usr/bin/demo --daemon --pidfile /run/demo.pid
ExecReload=/bin/kill -HUP $MAINPID
ExecStop=/bin/kill -TERM $MAINPID

[Install]
WantedBy=multi-user.target
